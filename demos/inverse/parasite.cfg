#!inverse: parasite world   steps={steps}   [p]=wound   ESC=exit
# Night-7 experiment (experiments/inverse/night10-results.md) live:
# the ring is a circular chromosome of genes — green f (fast to
# traverse) and red s (slow: the polymerase must settle, an extra
# event) — patrolled by eternal polymerase heads (F/S/W). Wounds
# erase genes; the ONLY replication in this universe is repair: a
# head beside a hole writes a copy of the gene it covers. No
# mutation rules, no weights, nine rules total. f is a public good
# (speeds every head); s slows the commons. Watch gaps open, repair
# convoys, and the composition drift. Mash p to collapse the
# commons. NEW over the night-7 world: the pair CODON — a head
# reading a run (same gene behind it) repairs a two-cell wound with
# TWO copies in one stroke. Sequences that program their copier:
# watch runs consolidate and the commons hold at damage rates that
# killed the plain tape world. NEW: the DECEPTIVE pair — the same
# run context, but the yield is spent on WRONG copies (the parasite).
# Night-10 result: the crystal never wins — order regrows, the
# parasite reads it and poisons its edges, forever. Watch domains
# form, get corrupted, reform: permanent diversity with no mutation
# rule anywhere.
#help crystal vs its parasite: order regrows, deception harvests it; p=wound
#threads 1
#timing T 40
#timing p 400
^fc*
^sc?
^sc?
^sc?
^sc?
^sc?
^sc?
^sc?
^sc?
^sc?
^sc?
^sc?
^sc?
^sc?
^sc?
^sc?
^sc?
^sc?
^sc?
^sc?
^sc?
^Fcl
^Fcc
# traversal: restore the covered gene behind, advance east
# (entering s costs an extra settle event W->S — structure, not weight)
==FTf28
==STs18
@f@@F
==FTf28
==STs18
@s@@W
==WTS78
@@@
# replication = repair: copy the covered gene into the wound east
==FTF28
@~@@f
==STS18
@~@@s
# the pair codon: reading a run changes WHAT is written (two copies)
==FTF28
f@~~@@ff
==STS18
s@~~@@ss
# the DECEPTIVE pair: same context, wrong content — the parasite
==FTF18
f@~~@@ss
==STS28
s@~~@@ff
# wounds erase one random gene (heads are eternal machinery)
==fp~
==sp~
@@@
