#!inverse: M4 scaffold oscillator   steps={steps}   [p]=destroy   ESC=exit
# Night-1 census discovery M4 (experiments/inverse/inverse-results.md):
# ==ATB writes a static B partner east on first fire; ==BTA fires only
# when B sees B east — so the partner it built is the load-bearing
# context. The oscillator constructs its own wall, then leans on it.
# The partner never fires (its east is empty). Press p to wound: M4
# has no repair rule — unlike demos/inverse/repair.cfg it dies.
#help yellow cell blinks beside its self-built wall; p kills it for good
#threads 1
#timing T 300
^Acc
==ATB38
@@@B
==BTA38
@B@@
==Ap~
@@@
==Bp~
@@@
