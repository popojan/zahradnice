#!{help} score:{score}
#help a/d move - w/e rotate - ESC quit
#timing g 500
#timing f 0
#control ~ pause
#grid 2 1
# Initial symbol must precede any rule header (parser double-classifies
# ^-lines that follow a rule, leaking the line text into the previous
# rule's body — see backlog/research/tetris-exercise-friction.md, B1).
^PcC
# === Playfield seed ===
# A single P at the screen centre (h-aligned to an even column via #grid 2 1).
# This rule fires once on the first f-tick: it draws the 24x22 perimeter
# (HH side walls, single-row top/bottom walls), erases P (field 3 = ~),
# and drops an R spawn token at piece-cell column 4 (standard tetris spawn
# column for I-piece, which spans piece-cells 4-7 of 10).
==Pf~
@@HHHHHHHHHHHHHHHHHHHHHHHH
  HH      R             HH
  HH                    HH
  HH                    HH
  HH                    HH
  HH                    HH
  HH                    HH
  HH                    HH
  HH                    HH
  HH                    HH
  HH          @         HH
  HH                    HH
  HH                    HH
  HH                    HH
  HH                    HH
  HH                    HH
  HH                    HH
  HH                    HH
  HH                    HH
  HH                    HH
  HH                    HH
  HHHHHHHHHHHHHHHHHHHHHHHH
# === Spawn: R + H above + 7 empty cells to the right -> I-horizontal ===
# Fires on f-tick whenever an R sits with the top wall directly above it
# and the next 7 cells to the right are empty.
==RfI
H
@~~~~~~~@@IIIIIII
# === Spawn: R + H above + T-shaped empty footprint -> T0 (stem-up) ===
# Sister rule to the I-H spawn — both anchor on R, both fire on f, the
# engine random-picks one when both LHS contexts pass. T0 piece is
# centred on R (top T at offsets (0, 0..+1), bottom row at (1, -2..+3))
# so that the piece's anchor (top-T's leftmost) ends up at R's column —
# matching the I-H spawn convention. Otherwise every T0 freeze would
# shift the spawn point right by 2 cols on the next cycle.
# R cell becomes T (top-T's leftmost), and 7 T literals fill the rest.
==RfT
  H
  @~  @  @T
~~~~~~ TTTTTT
# === R-walk: R + empty cell above -> R moves up one row ===
# Mutually exclusive with spawn (above is either ~ or H, not both).
==Rf~
~
@
@
R
@
# === I-horizontal: lateral move left (a key) ===
# Step 2 cols left (one piece-cell). Anchor cell (col 0) is INSIDE the new
# piece (cols -2..+5), so it stays I — field 3 = I.
==IaI
~~@IIIIIII@II@     ~~
# === I-horizontal: lateral move right (d key) ===
# Step 2 cols right. Anchor cell vacates (new piece at cols 2..9 doesn't
# include col 0) — field 3 = ~ erases it.
==Id~
@IIIIIII~~@@~      II
# === I-horizontal: rotate to vertical (w or e — symmetric for I) ===
# Stacked headers share one body. New I-vertical occupies offsets
# (-1..+2, +2..+3) — i.e., piece-col 2 of the original 4-cell I-H, rows
# centred on the anchor row. LHS requires the 6 non-overlap new cells empty.
==Iw~
==Ie~
    ~~          II
  @IIIIIII@   @~  ~~~~
    ~~          II
    ~~          II
# === I-horizontal: fall on game tick ===
# Anchor + 7 siblings to right disambiguate leftmost piece-cell.
# Below row all 8 cells empty -> piece moves down one row.
==Ig~
@IIIIIII
~~~~~~~~
@
@~~~~~~~
IIIIIIII
# === I-horizontal: freeze (8 sub-rules, one per blocking column) ===
# Each sub-rule fires when its specific (1, k) below cell is blocked
# (% matches field 6 = H or field 7 = # — wall or frozen block).
# Order-agnostic: when multiple cells are blocked, the engine picks any
# applicable sub-rule; all 8 produce identical RHS effect.
# LHS also requires (-1, 0) empty so the R-write doesn't overwrite a wall.
==Ig#78H#
~
@IIIIIII
%
@
R
@#######
==Ig#78H#
~
@IIIIIII
 %
@
R
@#######
==Ig#78H#
~
@IIIIIII
  %
@
R
@#######
==Ig#78H#
~
@IIIIIII
   %
@
R
@#######
==Ig#78H#
~
@IIIIIII
    %
@
R
@#######
==Ig#78H#
~
@IIIIIII
     %
@
R
@#######
==Ig#78H#
~
@IIIIIII
      %
@
R
@#######
==Ig#78H#
~
@IIIIIII
       %
@
R
@#######
# === I-vertical: fall on game tick ===
# Vertical body. Anchor + 1 right-sibling + 3 rows of II below. 2 ~ checks
# at offset (4, 0..1) for the row directly below the piece.
==Ig~
@I
II
II
II
~~
@
@~



II
# === I-vertical: freeze (2 sub-rules: col 0 or col 1 below blocked) ===
# NB: every body line is indented by one space so no row starts with `#`,
# which the parser would otherwise eat as a directive (see B7 in journal).
# All cells shift right by 1 col uniformly, so anchor-relative offsets
# are preserved.
==Ig#78H#
 ~
 @I
 II
 II
 II
 %
 @
 R
 @#
 ##
 ##
 ##
==Ig#78H#
 ~
 @I
 II
 II
 II
  %
 @
 R
 @#
 ##
 ##
 ##
# === I-vertical: lateral move left (a key) ===
# Step 2 cols left. New piece (cols -2..-1) disjoint from old (cols 0..1)
# so anchor cell vacates — field 3 = ~.
==Ia~
~~@I@II@~
~~II II~~
~~II II~~
~~II II~~
# === I-vertical: lateral move right (d key) ===
==Id~
@I~~@@~II
II~~ ~~II
II~~ ~~II
II~~ ~~II
# === I-vertical: rotate back to horizontal (w or e — symmetric) ===
# I-V at (0..3, 0..1) -> I-H at (1, -2..+5). Overlap (1, 0..1).
# Erase non-overlap old (rows 0, 2, 3 cols 0..1); write non-overlap new
# (row 1 cols -2..-1 and 2..5).
==Iw~
==Ie~
  @I      @  @~
~~II~~~~   II  IIII
  II         ~~
  II         ~~
# === T0: fall on game tick ===
# Vertical body. Anchor at top-T's leftmost. Top T at offset (0, 0..+1),
# bottom row at (1, -2..+3). Below row at (2, -2..+3) must be all empty.
# RHS erases old top T, leaves overlap row middle 2 cols alone, erases
# the 4 outer cols of the old bottom row, writes new bottom row.
==Tg~
  @T
TTTTTT
~~~~~~
@
  @~
~~  ~~
TTTTTT
# === T0: freeze (3 sub-rules, one per piece-cell pair below blocked) ===
# Indented by one space col so RHS `######` rows don't start with `#`
# (parser would otherwise eat them; see B7). Sub-rules differ only in
# the % column on body row 3 (which terminal col below is blocked).
==Tg#78H#
   ~
   @T
 TTTTTT
 %
@
   R
   @#
 ######
==Tg#78H#
   ~
   @T
 TTTTTT
   %
@
   R
   @#
 ######
==Tg#78H#
   ~
   @T
 TTTTTT
     %
@
   R
   @#
 ######
# === T0: lateral move left (a) ===
# Step 2 cols left. Anchor cell vacates -> field 3 = ~. New top T at
# (0, -2..-1), new bottom-row left-extension at (1, -4..-3). Old top T
# erased, old bottom-row right erased.
==Ta~
  ~~@T  @   TT@~
~~TTTTTT  TT    ~~
# === T0: lateral move right (d) ===
==Td~
  @T~~  @  @~TT
TTTTTT~~ ~~    TT
# === T0 -> T1 (w, CW): anchor preserved, erase (1, -2..-1), write (2, 0..+1) ===
==TwT
  @T  @  @
TTTTTT  ~~
  ~~      TT
# === T1 -> T0 (e, CCW): reverse — erase (2, 0..+1), write (1, -2..-1) ===
==TeT
  @T  @  @
  TTTT~~  TT
  TT      ~~
# === T1: fall on game tick ===
# T1 cells: (0, 0..+1), (1, 0..+3), (2, 0..+1). Below cells: (3, 0..+1)
# and (2, +2..+3). All 4 must be ~ for fall.
==Tg~
@T
TTTT
TT
~~  ~~
@
@~
~~~~
~~

TT
# === T1: freeze (2 sub-rules) ===
# Sub-rule A: left piece-cell-pair blocked at (3, 0..+1) — % at body (4, 1).
# Sub-rule B: stem-right piece-cell blocked at (2, +2..+3) — % at body (3, 5).
==Tg#78H#
 ~
 @T
 TTTT
 TT
 %
@
 R
 @#
 ####
 ##
==Tg#78H#
 ~
 @T
 TTTT
 TT
     %
@
 R
 @#
 ####
 ##
# === T1: lateral move left (a) ===
# T1 width is 4 cols; lateral L by 2 cols moves all 8 cells. New top at
# (0, -2..-1), new middle at (1, -2..+1), new bottom at (2, -2..-1).
# Old top (0, +0..+1), old middle (1, +2..+3), old bottom (2, +0..+1) erase.
==Ta~
~~@T  @  @T  ~~
~~TTTT  @  @TT
~~TT  @  @T  ~~
# === T1: lateral move right (d) ===
==Td~
@T~~  @  @  TT
TTTT~~  @ ~~TT
TT~~  @  @  TT