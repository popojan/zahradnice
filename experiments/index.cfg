#! Steps: {steps} ({parallel}) Moves: {moves} Score: {score} | {help}
# The template must be the very first line (grammar.cpp gates on it).
# A menu carries it so returning RESETS the status line: a program with
# its own #! sets it globally and nothing puts it back, so a demo's
# "pop=" caption would otherwise survive into everything opened later.
#help Living worlds  w/s choose  e run  q back
#timing T 0
#program 1 soc/forest2.cfg
#program 2 soc/forest-evo.cfg
#program 3 soc/forest-ladder.cfg
#program 4 soc/manna-watch.cfg
#program 5 convergence/contact.cfg
#program 6 ../programs/petri.cfg
#sound D sounds/clack.wav
#control Q return

^
^1cc?

# A curated few of experiments/ -- the ones that run unattended and stay
# alive. The rest of the tree is driven by a harness rather than watched:
# bandit/ and garden/ need their parameters fed in on a key, openworld/'s
# probes declare no timing at all, and forest.cfg goes extinct at step
# ~4300 and leaves an empty screen. Every program keeps its own #! so the
# status line still names what its counter means (pop=, grains=, ...).
#
# The anchor must be a character this rule does not itself paint, or it
# re-fires on every copy it just drew.
# --- selection ---
# `^1cc?` above is the caller's: on a return the engine plants the #program
# key of the entry we came back from, and `1` stands in on first entry.
# Each rule turns one key into the frame anchor Z — so the frame paints
# once, generically — plus the cursor in that key's row. The `~` either
# side of the key pins it as standing alone, so a rule cannot fire on a
# stray copy of its own key inside the painted frame.
# Forest fire
==1TZ
               ~@~
                @
x


                @

# Evolving forest
==2TZ
               ~@~
                @
x

                @

# Heritable trait
==3TZ
               ~@~
                @
x
                @

# Manna sandpile
==4TZ
               ~@~
                @
x               @

# Contact process
==5TZ
               ~@~
                @
                @
x

# Petri dish
==6TZ
               ~@~
                @
                @

x

==ZTe
   E V O L U T I O N .

  | |~Forest~fire~
  | |~Evolving~forest~
  | |~Heritable~trait~
@@| |~Manna~sandpil@~
  | |~Contact~process~
  | |~Petri~dish~
  | |~
  | |~Back

=Dxs~
 @ @@
| | x
=Dxw~
| | x
 @ @@

=1xex
@| Forest@@
=2xex
@| Evolving@@
=3xex
@| Heritable@@
=4xex
@| Manna@@
=5xex
@| Contact@@
=6xex
@| Petri@@
=Qxex
@| Back@@

=Qxq
@@@
