#! forest3  pop={score} steps={steps}
# Drossel-Schwabl forest fire, v2 (v1's vegetative growth densified the
# forest into one percolating cluster -> every strike spanned -> false
# negative from the MAPPING, not the substrate; see results.md).
# The DS correspondence needs site-wise regrowth anywhere, and the
# grammar cannot anchor on emptiness -- so emptiness becomes a symbol:
# ground g fills the field once (self-terminating spread), trees sprout
# from ground uniformly, burnout returns fire to ground. Extinction is
# no longer absorbing (ground always resprouts); the SOC question is
# whether the quasi-steady state shows broad/critical fire sizes with
# no tuned parameter.
# forest3: the refractory variant (user's suggestion). Burnout leaves
# ash `a` (dark red scar) on which nothing grows; ash slowly weathers
# back to ground. Fire + refractory tail = a Greenberg-Hastings-style
# excitable medium: look for travelling fronts and spiral waves rather
# than avalanches. The ash decay weight is the refractory-time knob.
# g = ground (dim), c = tree (green), A = fire (red/yellow), a = ash.
# Score: +1 sprout, -1 burnout => score = live cells (trees+fires).
#threads 1
#timing T 0
#help forest: watch | r reseed | q quit
#control q quit
#control r clear

^h**

# one-shot ground painter: the fill cannot carry colour, so fill a
# temp char and convert it once to dark (near-invisible) ground
==hTg08   0 1000
@@@

# growth: any ground cell sprouts a tree
==gTc20   1 1
@@@

# lightning: a tree spontaneously ignites (fractional weight: rarity
# without inflating every other weight)
==cTA13   0 0.03
@@@

# burn: fire spreads into an adjacent tree
==ATA13   0 200
@c@@A
==ATA13   0 200
c@@A@
==ATA13   0 200
c
@
@
A
@
==ATA13   0 200
@
c
@
@
A

# burnout: fire leaves refractory ash
==ATa10   -1 50
@@@

# weathering: ash slowly becomes fertile ground again
==aTg08   0 0.05
@@@

# --- watcher controls (carrier rules fire the engine actions) ---
=qgqg
@@@
=rgrg
@@@
